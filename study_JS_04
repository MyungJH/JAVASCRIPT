// プログラムの実行途中の値をログで確認する方法

// メソッド
console.log(値１, 値２, ...)  //　コンソールにメッセージを文字例する、戻り値はなし。
                             // 任意の値を出力できるので、プログラム途中で値がどのように変化しているかを知りたい時などに便利

// 例
const a = 10;
const b = 30;
const sum = a + b;
console.log(sum); // 結果：４０

// console.log()メソッドの引数には複数の値をカンマ区切りで設定できる
console.log(`Hello`, `NoW`, new Date());
